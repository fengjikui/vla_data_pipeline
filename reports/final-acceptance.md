# 阶段交付验收记录

验证日期：2026-09-12。预定汇报：2026-09-14。数据发布 ID：`d6c42e269fafb49808f8863b`。

## 工程结果

| 验收内容 | 结果与证据 |
|---|---|
| 固定来源与有限下载 | 3 个来源，各 6 条 episode；原始缓存 212,232,535 字节；revision、SHA256 和许可在 `configs/demo_sources.lock.json` |
| 多来源实际处理 | G1 上身遥操作、YAM 双臂遥操作、Panda 双臂仿真；合计 23,109 个数值时刻、590 个窗口 |
| 时间与标签 | 视频按源时间取当前及过去帧；阶段语言只取已生效标签；保留源时钟假设与待核实语义 |
| 分割与统计 | origin_group 稳定切分；统计仅来自训练轨迹；尾部 mask；动作窗口不跨 episode |
| 归一化往返 | 最大绝对误差约 `9.55e-08`；见 `acceptance.json` |
| 异常与重复 | 人为注入 3 类错误均隔离；1 个数值完全重复副本跳过；不将注入比例当原始缺陷率 |
| 增量与版本 | 新增本地同契约数据后，旧轨迹 split 不变，旧发布仍通过完整性校验；不同字节不能覆盖同 revision 原件 |
| 离线与复用 | 已缓存依赖和原始数据时，无网络重跑复用相同 release；输出 SHA256 全部通过 |
| 小模型训练 | 三组独立随机初始化多模态 BC，CPU 各 120 步；梯度有限，检查点恢复输出误差为 0，优化器状态可恢复 |
| 本机测试与打包 | macOS ARM64 / Python 3.12，21 项测试通过，wheel 构建成功 |
| Ubuntu 24.04 | 21 项测试通过，无跳过；CLI 与 wheel 构建通过 |
| Windows runner | 21 项测试通过，无跳过；CLI 与 wheel 构建通过 |

跨平台实际运行记录：[GitHub Actions](https://github.com/fengjikui/vla_data_pipeline/actions/runs/34678405092)，对应工程提交 `f75695c52761ca349cfc12b6bf1412775e636f1b`。下载的 JUnit 结果汇总见 [test-summary.json](test-summary.json)，任务状态见 [cross-platform-ci.json](cross-platform-ci.json)。CI 使用生成的合成视频与契约样本，不下载约 212 MB 的公开数据，不训练 Torch 模型。

## 交付材料

- 5 篇核心研究与设计报告，24 项数据条目、57 项基础调研来源；后续部署文档补充运行库官方出处。
- 16 页可编辑中文 PPTX，备用 PDF；逐页讲稿、3 分钟短版、学习指南与问答。
- 内嵌三张原始样本图的离线 HTML 报告，图片出处及许可单独记录。
- 公司环境下载优先级、Ubuntu / Windows 与内网迁移说明、现场任务卡、四周建议安排。
- 可运行源码、版本锁、测试、CI、有限下载清单、无需原始数据的校验迁移 ZIP 生成器。

## 已验证范围与待验证范围

当前训练出口是 `interface_smoke`：可以用于数据和训练代码的接口检查。尚未核实全部物理动作语义，也没有输出已验证兼容 openpi / GR00T 的生产训练集。完整预训练 VLA 微调、仿真闭环、机器人执行、工业合格率与数据增益均未完成，不能据本次 loss 下降作能力结论。

Mac 的真实样本与训练结果、CI 的合成契约测试，是不同范围的证据。公司服务器仍需验证真实样包、存储、具体编码、驱动、SDK、内网依赖和所选模型。核心项目与数据包可迁移；Python 环境、wheel、GPU 依赖要按 OS / 架构分别准备。

## 演示材料验证说明

PPTX 经过结构、布局、原生表格和重新导入检查，并对页面图片逐页目检。PDF 由随附运行环境的 LibreOffice 导出，显式提供中文字体配置，再逐页渲染检查。最终 16 页视觉检查通过，中文文字提取也全部通过；最终文件哈希见 [artifact-validation.json](artifact-validation.json)。没有在用户公司的 PowerPoint 中验证字体替换；备用 PDF 用于固定版式展示。

离线 HTML 不依赖外部脚本、字体或图片；内容与图片嵌入由脚本检查。当前浏览器工具禁止打开本地文件，因此未声称完成浏览器视觉验收。汇报前请按演示手册在实际电脑打开一次；PDF 已提供固定版式替代材料。

## 复现入口

按 [README](../README.md) 安装与运行。完整演示顺序见 [演示手册](../docs/08-demo-runbook.md)，新来源登记见 CLI `register-local --help`。样本扩量与公司下载清单见 [下载计划](../docs/11-download-priorities.md)。
